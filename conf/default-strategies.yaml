strategies:
  admin:
    scheme: 'password'
    mode: false
    options:
      password: '{{admin.password}}'
      salt: 'salt'
      endpoint: '/admin/login'
      successEndpont: '/admin'
      cookieName: '{{name}}-admin'
      ttl: 2592000000 #30 days
      loginForm:
        name: '{{prettyName}} Admin'
